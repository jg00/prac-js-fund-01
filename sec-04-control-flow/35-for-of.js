console.log("--for of--");
// Will give you the values instead of the index

// const colors = ["red", "green", "blue"];

for (let color of colors) {
  console.log(color); // values instead of index returned
}
